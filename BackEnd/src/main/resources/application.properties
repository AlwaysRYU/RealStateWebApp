
#db datasource
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://localhost:3306/happyhouse_rf?serverTimezone=Asia/Seoul
spring.datasource.username=root
spring.datasource.password=7514

#mybatis
mybatis.config-location=classpath:/config/mybatis-config.xml

# file upload
# Enable multipart uploads
spring.servlet.multipart.enabled=true
# Max file size.
spring.servlet.multipart.max-file-size=200MB

# json converter
spring.mvc.converters.preferred-json-mapper=gson

# here, we can construct var
# we can use @value( "${ name }" ) <-- like tihs
app.fileupload.uploadPath=C:\\SSAFY_SpringBoot\